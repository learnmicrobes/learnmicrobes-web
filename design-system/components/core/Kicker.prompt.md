Uppercase, letter-spaced eyebrow label sitting above headlines (QUESTION, CLINICAL BENCH REFERENCE, FREE TO START).

```jsx
<Kicker tone="sage">Question</Kicker>
<Kicker tone="onDark">Free to start</Kicker>
```

Tones: `sage` · `teal` · `muted` · `onDark` · `onDarkMuted`. Always written in sentence/any case — it is force-uppercased by CSS.
