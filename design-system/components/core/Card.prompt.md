The base white surface — soft low-spread shadow, teal hairline border, rounded corners. Everything modular sits in one.

```jsx
<Card interactive pad="md" radius="md">…</Card>
```

Props: `interactive` (calm hover rise), `pad` none|sm|md|lg, `radius` sm|md|lg|xl (md = product, lg = editorial/social).
