Frames real educational imagery (plates, microscopy) in a rounded white card with a soft shadow and an optional teal specimen tag.

```jsx
<SpecimenFrame src="plate.jpg" alt="Blood agar" label="Specimen A" />
```

Pass `children` instead of `src` to frame arbitrary content. `ratio` sets aspect ratio.
