A/B/C/D answer card with a sage letter badge. Drives both quiz posts and the practice tool.

```jsx
<QuizOption letter="A">H₂S production</QuizOption>
<QuizOption letter="B" state="correct">Gas production</QuizOption>
```

States: `default` · `selected` · `correct` · `incorrect`.
