Compact figure + label for credibility stats, with an optional icon tile.

```jsx
<StatChip icon="fa-solid fa-rotate" value="145+" label="quiz reps" />
<StatChip value="24 h" label="pure growth" onDark />
```

Use `onDark` on deep-green surfaces.
